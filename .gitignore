.DS_Store
backup.tar.gz
result
iso/nixos.iso

# Encrypted secrets: safe to commit (age-encrypted), but kept out of the
# public repo as defense-in-depth against future cryptographic breaks.
# On the VM, Makefile targets `git add -f` this file so the flake can see it.
machines/secrets.yaml

# Generated during bootstrap -- tracked on purpose (public keys only).
# This negation lets `git add` work without -f.
!machines/generated/
machines/generated/*
!machines/generated/host-authorized-keys
!machines/generated/vm-age-pubkey

# bv (beads viewer) local config and caches
.bv/

.opencode/skills/*
.opencode/command/*
.opencode/command/*
.github/skills/*
.github/agents/*
.github/prompts/*
.github/command/*
.logs
.vscode